{#{% set footer = 'Je ne suis pas un footer.' %}#}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
        {% block navbar %}
            {{ include('includes/_navbar.html.twig', {}, with_context = false) }}
        {% endblock %}
        </header>
        <main>
        {% block body %}{% endblock %}
        </main>
        {{ render(controller('App\\Controller\\MovieController::decades', {'max': 5})) }}
{#    {{ include('includes/_footer.html.twig', {footer: 'Je suis un footer.'}, with_context = false) }}#}
    </body>
</html>
